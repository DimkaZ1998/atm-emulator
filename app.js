'use strict' ;

let balance = 10000;

while (true) {
    const menu = `
    Добро поаловать в банкомат!
    ваш выбор:
    1. Показать баланс
    2. Снясть средства
    3. Пополнить счет
    4. Выход
    `;

    const choice = prompt(menu);

    switch (choice) {
        case '1':
            alert(`Dfi ,fkfyc: ${баланс} ₽`)
            break;
    }
}